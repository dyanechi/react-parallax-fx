/// <reference types="react" />
import { IParallaxProps } from '../@types';
export declare function Parallax({ startScroll, endScroll, speed, opacity, transform, offset, background, gradient, filter, disabled, children }: IParallaxProps): JSX.Element;
export default Parallax;
